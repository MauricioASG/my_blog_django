# my_blog_django

Proyecto de práctica con Django (4.2 LTS) para reforzar fundamentos.

## Requisitos
- Python 3.11+

## Instalación (Windows)
```bash
python -m venv envs/my_blog
envs\my_blog\Scripts\activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
